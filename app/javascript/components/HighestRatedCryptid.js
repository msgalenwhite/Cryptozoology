import React from 'react';

const HighestRatedCryptid = props => {
  // let categories =['Aliens', 'Unicorns', 'Bigfoot', 'Lochness', 'Betty']
  //
  // let cryptids = categories.map((cryptid) => {
  //   <li>{cryptid}</li>
  // })

  return(
    <div>
      <ol>
        HighestRatedCryptid HERE!
      </ol>
    </div>
  );
};

export default HighestRatedCryptid;
